<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<link rel="stylesheet" href="/global.css" />
		<script src="https://cdn.tailwindcss.com"></script>
		<title>Edit <%= ingredient.name %></title>
	</head>
	<body class="bg-slate-100 h-dvh">
		<%- include('../partials/_navbar.ejs') %>
		<section class="container mx-auto">
			<div class="flex items-center justify-center h-dvh">
				<div class="bg-white w-fit p-16 rounded-3xl flex flex-col gap-8">
					<h1 class="font-semibold text-4xl"><%= ingredient.name %></h1>
					<form
						action="/ingredients/<%= ingredient._id %>?_method=PUT"
						method="POST"
						class="flex flex-col gap-8 w-96"
					>
						<div class="flex flex-col gap-2">
							<label for="name">Ingredient Name</label>
							<input
								type="text"
								id="name"
								name="name"
								class="bg-[#F7FBFF] border border-[#D4D7E3] rounded-lg max-h-10 px-2 py-3 placeholder:font-extralight placeholder:text-sm"
								value="<%= ingredient.name %>"
								required
							/>
						</div>
						<button
							type="submit"
							class="bg-[#162D3A] text-base text-center text-white font-normal rounded-lg py-1 px-4 h-fit hover:animate-pulse"
						>
							Update Ingredient
						</button>
					</form>
				</div>
			</div>
		</section>
	</body>
</html>
